import { NextResponse } from 'next/server';

// TODO: Sprint 1 で実装（楽天GORA API プロキシ）
export async function GET() {
  return NextResponse.json(
    { error: 'Not implemented: Sprint 1 で実装予定' },
    { status: 501 }
  );
}
